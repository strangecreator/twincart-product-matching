enabled: true

# number of nearest neighbors used to expand each query embedding
topk: 10

# alpha coefficient in: q' = normalize(q + alpha * mean(nn))
alpha: 3.0

# iterations (some pipelines do 1–2 passes; start with 1)
iters: 1
